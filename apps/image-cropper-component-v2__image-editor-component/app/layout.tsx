export const metadata = { title: "image-cropper-component-v2-image-editor-component" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
