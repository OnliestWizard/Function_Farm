/**
 * Assembled App — generated by GitSort (deterministic multi-edge assembler).
 *
 * Walks a path through the Capability Graph and wires 4 capabilities into one
 * instance; a value flows along each seam:
 *   React Image Dropzone → onUpload(ImageDataURL) → Definitive Self-Contained Camera Capture Component → Image Cropper Component (react-easy-crop) → Image Editor Component
 *
 * Seams (3 wired, avg score 0.983):
 *   image-dropzone-upload.onUpload → camera-capture-definitive.imageType : ImageDataURL
 *   camera-capture-definitive.onCapture → image-cropper-component-v2.image : ImageDataURL
 *   image-cropper-component-v2.onCropped → image-editor-component.image : ImageDataURL
 *
 * Runtime-agnostic; wrap in a project template (web/RN) to build & deploy.
 */
import React, { useState } from 'react';
import ImageDropzoneUpload from './ImageDropzoneUpload';
import DefinitiveCameraCapture from './DefinitiveCameraCapture';
import ImageCropper from './ImageCropper';
import ImageEditor from './ImageEditor';

export default function AssembledApp() {
  const [imageType, setImageType] = useState<string>('');
  const [image, setImage] = useState<string>('');
  const [image2, setImage2] = useState<string>('');

  return (
    <>
      <ImageDropzoneUpload onUpload={(value) => setImageType(value)} />
      <DefinitiveCameraCapture imageType={imageType} onCapture={(value) => setImage(value)} />
      <ImageCropper image={image} onCropped={(value) => setImage2(value)} />
      <ImageEditor image={image2} />
    </>
  );
}
