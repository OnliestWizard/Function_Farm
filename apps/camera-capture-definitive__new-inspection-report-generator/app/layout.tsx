export const metadata = { title: "camera-capture-definitive-new-inspection-report-generator" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
