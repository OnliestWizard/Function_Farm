export const metadata = { title: "react-easy-crop-self-contained-photo-download-app" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
